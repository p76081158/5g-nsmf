forecastingBlock:
  - block: 1           # block indes of slice (slice split to multiple blocks)
    duration: 100      # block up time
    resource: 600      # required resources (cpu, bandwidth)
  - block: 2
    duration: 100
    resource: 300