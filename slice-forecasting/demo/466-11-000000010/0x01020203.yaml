forecastingBlock:
  - block: 1           # block index of slice (slice split to multiple blocks)
    duration: 100      # block time length
    cpu: 600           # required resources (cpu, bandwidth)
    bandwidth: 3
  - block: 2
    duration: 100
    cpu: 300
    bandwidth: 3